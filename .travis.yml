language: python

python:
- "2.7"
- "3.5"
    
install:
- pip install -r requirements.txt

before_script:
- python setup.py install

script: 
- py.test --cov-report term-missing --cov=peakdet

after_success:
- coveralls
